# Chacra — Architecture

Living technical reference. Owned by CTO. Evolves as we build.
Product requirements live in SPEC.md. Progress lives in ROADMAP.md.

---

## Directory Structure

All source code lives under `src/`. Config files stay at root.

```
src/
  app/
    layout.tsx                   # Root layout (existing)
    page.tsx                     # Landing page (existing, keep as-is)
    globals.css                  # Tailwind + semantic tokens (existing)

    demo-cooperativas/           # Static demo (existing, keep as-is)
    demo-financieras/            # Static demo (existing, keep as-is)

    producer/                    # Producer PWA (to build)
      layout.tsx                 # Producer shell + offline detection
      page.tsx                   # Transaction form + sign-in
      history/page.tsx           # Transaction history

    dashboard/                   # Cooperative dashboard (to build)
      layout.tsx                 # Dashboard shell + auth guard
      page.tsx                   # KPIs, charts, traceability
      producers/page.tsx         # Producer list with filters
      producer/[id]/page.tsx     # Individual producer detail
      settings/page.tsx          # Products, invite code, export goals

    scoring/                     # Financiera scoring (to build)
      layout.tsx                 # Scoring shell + auth guard
      page.tsx                   # Portfolio view
      producer/[id]/page.tsx     # Individual credit profile

    api/
      auth/[...all]/route.ts     # better-auth catch-all
      sync/route.ts              # PWA batch sync endpoint

  lib/
    db.ts                        # Neon connection + Drizzle client
    auth.ts                      # better-auth config + session helpers
    scoring.ts                   # Credit score computation
    sync.ts                      # Batch validation, UUID dedup
    prices.ts                    # Price benchmarks per crop/region
    integrity.ts                 # Anomaly detection, trust scores
    utils.ts                     # cn() helper (existing)

  db/
    schema.ts                    # Drizzle schema (single source of truth)
    index.ts                     # Re-exports Drizzle client
    migrations/                  # Generated by drizzle-kit

  actions/
    transactions.ts              # Server Actions: create, confirm, correct
    producers.ts                 # Server Actions: register, update profile
    cooperatives.ts              # Server Actions: manage coop, products, goals

  components/
    ui/                          # shadcn/ui (existing)
    landing/                     # Landing page components (existing)

  hooks/
    use-mobile.ts                # Existing

# Root config files
biome.jsonc                      # Lint + format (replaces ESLint + Prettier)
components.json                  # shadcn/ui config
drizzle.config.ts                # Drizzle-kit config (Phase 1)
next.config.mjs                  # Next.js config
tsconfig.json                    # TypeScript config
package.json
```

## Tooling

| Tool | Purpose | Replaces |
|------|---------|----------|
| Biome | Lint + format | ESLint + Prettier |

Biome config: tabs, double quotes, organized imports. Lint excludes `src/components/ui/*` (shadcn-generated).

---

## Database Schema

### Strategy

better-auth manages 4 core tables (user, session, account, verification). We extend the `user` table
via `additionalFields` (no separate profile table). The `cooperative` and `transaction` tables are ours.

Auth tables generated by `npx @better-auth/cli generate --output src/db/auth-schema.ts`.
Our tables defined manually in `src/db/schema.ts` which re-exports auth-schema.

### cooperative

| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| id | TEXT | PK | crypto.randomUUID() |
| name | TEXT | NOT NULL | Display name |
| region | TEXT | NOT NULL | Geographic region |
| invite_code | TEXT | NOT NULL, UNIQUE | 6-char uppercase alphanumeric |
| product_list | TEXT[] | NOT NULL, DEFAULT '{}' | PostgreSQL native array |
| export_goals | JSONB | NOT NULL, DEFAULT '{}' | `Record<string, { targetKg: number }>` |
| created_at | TIMESTAMP | NOT NULL, DEFAULT NOW() | |

### transaction

| Column | Type | Constraints | Notes |
|--------|------|-------------|-------|
| id | SERIAL | PK | Auto-increment |
| uuid | TEXT | NOT NULL, UNIQUE | Client-generated, ON CONFLICT DO NOTHING |
| producer_id | TEXT | NOT NULL, FK(user.id) | |
| cooperative_id | TEXT | NOT NULL, FK(cooperative.id) | Denormalized for query perf |
| product | TEXT | NOT NULL | Must be in cooperative's productList |
| quantity_kg | NUMERIC(10,2) | NOT NULL | Kilograms |
| price_per_kg | NUMERIC(10,2) | NOT NULL | PEN/kg |
| buyer | TEXT | nullable | Optional |
| date | DATE | NOT NULL | YYYY-MM-DD |
| integrity_status | TEXT | NOT NULL, DEFAULT 'pending' | 'confirmed' / 'flagged' / 'pending' |
| created_at | TIMESTAMP | NOT NULL, DEFAULT NOW() | Server receive time |
| synced_at | TIMESTAMP | NOT NULL, DEFAULT NOW() | |

Indexes: producer_id, cooperative_id, date

### user (additional fields via better-auth)

| Field | Type | Default | input | Notes |
|-------|------|---------|-------|-------|
| role | enum | null | false | 'producer' / 'cooperative' / 'financiera'. Set by server only. |
| cooperativeId | string | null | false | FK to cooperative |
| producerName | string | null | true | Display name (may differ from Google) |
| producerRegion | string | null | true | Producer's region |

### Relations

```
cooperative 1---* user (via user.cooperativeId)
user 1---* transaction (via transaction.producerId)
cooperative 1---* transaction (via transaction.cooperativeId)
```

---

## Auth Design

### Role assignment: deferred to onboarding (not OAuth hooks)

1. User signs in with Google → better-auth creates user with `role = null`
2. `callbackURL` sends them back to entry point (`/productor`, `/dashboard`, `/scoring`)
3. Layout checks: no role? → show onboarding form for that entry point
4. Onboarding server action sets role + associated data

This is robust because:
- No fragile coupling between OAuth state and role
- URL the user visits IS the role selector
- Works across devices

### Session enrichment

`customSession` plugin adds role, cooperativeId, producerName, producerRegion to every session response.
No extra DB query needed for role checks.

### Layout auth pattern

Each section layout handles 4 states:
- **No session** → inline sign-in card (not redirect)
- **Session, no role** → inline onboarding form
- **Session, wrong role** → "wrong role" notice with link
- **Session + correct role** → render children

### Helpers

```
getSession()          → Session | null (server-side)
requireAuth()         → Session (redirects if null)
requireProducer()     → Session (redirects if not producer)
requireCooperative()  → Session (redirects if not cooperative)
requireFinanciera()   → Session (redirects if not financiera)
```

---

## Module Contracts

### `lib/prices.ts`
```
getPriceBenchmark(product, region)        → PriceBenchmark | null
getPriceSignal(pricePerKg, product, region) → "above" | "below" | "at" | null
getBenchmarksForCooperative(cooperativeId) → Map<string, PriceBenchmark | null>
```
Returns null if < 5 data points. Excludes flagged transactions. 90-day window.

### `lib/integrity.ts`
```
checkTransaction(transactionId)           → IntegrityFlag[]
computeTrustScore(producerId)             → TrustScoreResult
checkCrossValidation(producerId, coopId, period) → IntegrityFlag[]
```
Trust formula: `clamp(50 + confirmed - 5*flagged, 0, 100)`. New producer = 50.

### `lib/scoring.ts`
```
computeCreditScore(producerId)            → CreditScore
```
Tier A: 6+ months, 500+ PEN avg, 70+ trust, 60+ consistency.
Tier B: 3+ months, 200+ PEN avg, 40+ trust. Tier C: everything else.
Loan range: A = 2-6x monthly, B = 1-3x monthly, C = null.

### `lib/sync.ts`
```
syncBatch(producerId, coopId, transactions) → SyncResponse
```
Validates, deduplicates by UUID (ON CONFLICT DO NOTHING), runs integrity checks.
Max 100 per batch (enforced at API route level).

### `actions/transactions.ts`
```
createTransaction(input)                  → ActionResult<{ id, uuid }>
getTransactionsByProducer(producerId?, opts) → ActionResult<{ transactions, total }>
getTransactionsByCooperative(opts)        → ActionResult<{ transactions, total }>
confirmTransaction(transactionId)         → ActionResult<void>
rejectTransaction(transactionId)          → ActionResult<void>
```

### `actions/producers.ts`
```
getProducersForCooperative(opts?)          → ActionResult<{ producers: ProducerListItem[] }>
getProducerProfile(producerId)             → ActionResult<ProducerProfile>
```

### `actions/cooperatives.ts`
```
addProduct(name)                           → ActionResult<void>
removeProduct(name)                        → ActionResult<void>
setExportGoal(product, targetKg)           → ActionResult<void>
removeExportGoal(product)                  → ActionResult<void>
getCooperativeStats()                      → ActionResult<CooperativeStats>
```

### Shared types: `lib/types.ts`
All module interfaces use types from this file. Key shapes: `CreditScore`, `PriceBenchmark`, `IntegrityFlag`, `SyncResponse`, `CooperativeStats`, `ProducerListItem`.

---

## Technical Decisions Log

| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-03-19 | Biome replaces ESLint + Prettier | Single tool, faster, tabs + double quotes |
| 2026-03-19 | All source under src/ | Clean separation, matches template conventions |
| 2026-03-19 | Extend user via additionalFields, not separate table | Single query for auth+role, no JOINs, officially supported |
| 2026-03-19 | Role deferred to onboarding, not OAuth hooks | Simpler, reliable, no coupling to OAuth state |
| 2026-03-19 | neon-http driver (not WebSocket) | Stateless, no pooling, ideal for Vercel serverless |
| 2026-03-19 | cooperativeId denormalized on transaction | Avoids JOIN through user for coop dashboard queries |
| 2026-03-19 | numeric(10,2) for money/quantity | Exact decimal arithmetic, no floating point |
| 2026-03-19 | Inline sign-in (not /auth/* routes) | Per SPEC: sign-in lives within each section |

---

## Dependencies

### To install (Phase 1)

```
# Database
@neondatabase/serverless
drizzle-orm
drizzle-kit (dev)

# Auth
better-auth

# Offline (Phase 3)
dexie
```

### Already installed

```
next@16.1.7, react@19.2.4, react-dom@19.2.4
shadcn@4.0.8, @radix-ui, recharts, sonner, next-themes
lucide-react, @solar-icons/react
tailwindcss@4.2.1, typescript@5.9.3
```
